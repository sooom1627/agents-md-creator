# 現状の実装における問題点

最終更新: 2026-01-07
調査ブランチ: `claude/document-implementation-issues-2jm32`

---

## 📊 概要

このドキュメントは、Agents.md Master Generator プロジェクトの現状実装における問題点を体系的にまとめたものです。

**全体ステータス:**
- ✅ テスト: 89/89 パス
- ✅ 型チェック: エラーなし
- ✅ Lint: エラーなし
- ⚠️ 機能実装: 部分的（約33%完了）

---

## 🔴 重大な問題

### 1. ウィザードステップの大幅な未実装

**問題:**
- `TOTAL_STEPS = 6` と定義されているが、実装済みは **Step 1-2のみ（33%）**
- Step 3, 4, 5, 6 が完全に未実装

**影響:**
- ユーザーがウィザードを完了できない
- agents.md ファイルの生成が不完全

**実装状況:**
| ステップ | 状態 | ページパス | 内容 |
|---------|------|-----------|------|
| Step 1  | ✅ 実装済み | `/app/wizard/page.tsx` | プロジェクト基本情報 |
| Step 2  | ✅ 実装済み | `/app/wizard/step2/page.tsx` | AIの役割 |
| Step 3  | ❌ 未実装 | - | （未定義） |
| Step 4  | ❌ 未実装 | - | （未定義） |
| Step 5  | ❌ 未実装 | - | （未定義） |
| Step 6  | ❌ 未実装 | - | （未定義） |

**該当ファイル:**
- `src/features/wizard/types/wizard-form.ts:22-24`
- `src/features/wizard/context/wizard-context.tsx:26`

---

### 2. コア機能の欠落

#### 2.1 エクスポート/ダウンロード機能の未実装

**問題:**
- agents.md ファイルをダウンロードする機能が存在しない
- `features/export/` ディレクトリが存在しない

**期待される機能:**
- Markdown ファイルのダウンロード
- クリップボードへのコピー
- GitHub への直接コミット機能（将来的）

**影響:**
- ユーザーが生成したファイルを取得できない
- プロジェクトの主要な価値提供ができない

---

#### 2.2 プレビュー機能の不完全実装

**問題:**
- リアルタイムプレビューは実装されているが、独立したプレビューページが未実装
- `app/preview/` および `features/preview/` が存在しない

**現状:**
- ✅ ウィザード内の右側パネルにプレビュー表示（`PreviewArea` コンポーネント）
- ❌ 独立したプレビューページ
- ❌ フルスクリーンプレビュー
- ❌ シンタックスハイライト（react-markdown未使用）

**該当ファイル:**
- `src/features/wizard/components/preview-area.tsx:1-16`

---

#### 2.3 テンプレート機能の不完全実装

**問題:**
- テンプレート選択UIは実装されているが、テンプレート適用機能が未実装
- `useSearchParams` でクエリパラメータ `?template=xxx` を処理していない

**現状:**
```typescript
// src/app/template/page.tsx:43
<Link href={`/wizard?template=${selectedTemplateId}`}>
  次へ進む
</Link>
```
上記のリンクはあるが、受け取り側（`/wizard`）で処理していない。

**影響:**
- ユーザーがテンプレートを選択しても反映されない
- 「テンプレートから始める」の価値提供ができない

**該当ファイル:**
- `src/app/template/page.tsx:1-60`
- `src/app/wizard/page.tsx:1-71`（クエリパラメータ処理なし）
- `src/features/wizard/constants/templates.ts:1-96`

---

## 🟡 設計・アーキテクチャの問題

### 3. プロジェクト構造の不一致

**問題:**
CLAUDE.md で定義された理想的な構造と実際の実装が異なる。

**期待される構造（CLAUDE.md）:**
```
src/
├── app/
│   ├── page.tsx
│   ├── generator/      ← 期待
│   │   └── page.tsx
│   └── preview/        ← 期待
│       └── page.tsx
├── features/
│   ├── wizard/
│   ├── preview/        ← 期待
│   └── export/         ← 期待
```

**実際の構造:**
```
src/
├── app/
│   ├── page.tsx
│   ├── wizard/         ← 実装（generator ではない）
│   │   ├── page.tsx
│   │   └── step2/page.tsx
│   └── template/       ← 実装（予定外）
├── features/
│   └── wizard/         ← wizardのみ
```

**影響:**
- ドキュメントと実装の乖離
- 新規開発者の混乱
- 保守性の低下

---

### 4. 技術スタックの不一致と未使用依存関係

#### 4.1 インストール済みだが未使用の依存関係

**問題:**
以下のパッケージがインストールされているが、コードベースで全く使用されていない（0件）。

| パッケージ | 状態 | 使用箇所 | package.json |
|-----------|------|---------|--------------|
| `@tanstack/react-query` | ❌ 未使用 | 0件 | `^5.90.16` |
| `react-toastify` | ❌ 未使用 | 0件 | `^11.0.5` |
| `react-hook-form` | ❌ 未使用 | 0件 | `^7.70.0` |
| `@hookform/resolvers` | ❌ 未使用 | 0件 | `^5.2.2` |
| `zod` | ❌ 未使用 | 0件 | `^4.3.5` |

**実際のフォーム管理:**
- React の `useState` による手動管理
- バリデーションなし

**影響:**
- バンドルサイズの無駄な増加
- 技術スタックの混乱
- フォームバリデーションの欠如

**該当ファイル:**
- `src/features/wizard/context/wizard-context.tsx:49-112` (useState による実装)
- `package.json:17-28`

---

#### 4.2 不足している依存関係

**問題:**
CLAUDE.md で必須とされているが、インストールされていない。

| パッケージ | 期待される用途 | CLAUDE.md |
|-----------|--------------|-----------|
| `zustand` | ステート管理 | line 78 |
| `react-markdown` | プレビューのMarkdown表示 | line 77 |
| `rehype-*` | シンタックスハイライト | line 77 |

**現状の実装:**
- ステート管理: React Context（`WizardProvider`）
- Markdown表示: `CodeBlock` コンポーネント（シンタックスハイライトなし）

**影響:**
- CLAUDE.md との技術スタック不一致
- シンタックスハイライトの欠如
- ステート管理の拡張性不足

---

#### 4.3 shadcn/ui のセットアップ不完全

**問題:**
- `src/components/` ディレクトリが存在しない
- `components.json` が存在しない
- shadcn/ui コンポーネントが未インストール

**package.json には存在:**
```json
"shadcn/ui + Tailwind CSS(v3)"  // CLAUDE.md
```

**実際:**
- Tailwind CSS v4 を使用（問題なし）
- shadcn/ui コンポーネントは未使用
- すべて手作りコンポーネント

**影響:**
- アクセシビリティ標準への準拠が不完全な可能性
- 開発速度の低下
- UIコンポーネントの品質不一致

---

### 5. フォーム管理とバリデーションの欠如

**問題:**
React Hook Form + Zod が計画されているが、実装は `useState` のみ。

**現状の問題点:**
```typescript
// src/features/wizard/context/wizard-context.tsx:114-129
const toggleRole = useCallback((roleId: AIRolePreset) => {
  setSelectedRoles((prev) => {
    if (prev.includes(roleId)) {
      return prev.filter((id) => id !== roleId)
    }
    // ⚠️ ハードコードされた制限
    if (prev.length >= 3) {
      return prev
    }
    return [...prev, roleId]
  })
}, [])
```

**欠けている機能:**
- ✅ フォームバリデーション（Zod schema）
- ✅ エラーメッセージ表示
- ✅ フォーム送信処理
- ✅ フィールド単位の検証

**影響:**
- ユーザビリティの低下
- バリデーションロジックの分散
- 保守性の低下

---

## 🟢 軽微な問題

### 6. コードスタイルとベストプラクティス

#### 6.1 ファイルサイズの遵守

**CLAUDE.md の規定:**
> 50-200行を維持、最大300行

**実際のファイルサイズ:**
```
195行: src/features/wizard/context/wizard-context.tsx  ✅
148行: src/features/wizard/utils/generate-markdown.test.ts  ✅
128行: src/features/wizard/components/role-card.test.tsx  ✅
125行: src/features/wizard/components/role-card.tsx  ✅
```

**評価:** ✅ 適切に分割されている

---

#### 6.2 LocalStorage の実装

**現状:**
- ✅ 自動保存機能は実装済み
- ✅ 復元機能も実装済み

```typescript
// src/features/wizard/context/wizard-context.tsx:145-152
useEffect(() => {
  if (typeof window === 'undefined') return
  const dataToSave = {
    currentStep,
    ...formData,
  }
  localStorage.setItem(STORAGE_KEY, JSON.stringify(dataToSave))
}, [currentStep, formData])
```

**評価:** ✅ 正しく実装されている

---

## 📋 優先度別改善リスト

### 🔥 P0 - 緊急（プロダクトとして機能しない）

1. **エクスポート/ダウンロード機能の実装**
   - agents.md ファイルのダウンロード
   - クリップボードへのコピー
   - 影響: プロダクトの主要価値を提供できない

2. **ウィザードステップ3-6の実装**
   - 残り4ステップの設計と実装
   - 影響: ウィザードフローが完結しない

3. **テンプレート適用機能の実装**
   - クエリパラメータの処理
   - テンプレートからフォームデータへの変換
   - 影響: 「テンプレートから始める」が機能しない

---

### ⚠️ P1 - 高（品質とUXに影響）

4. **React Hook Form + Zod への移行**
   - フォームバリデーションの実装
   - エラーハンドリングの改善
   - 影響: UX低下、バグのリスク

5. **react-toastify の統合**
   - 成功/エラー通知の実装
   - 影響: ユーザーフィードバックの欠如

6. **shadcn/ui のセットアップと導入**
   - `components.json` の作成
   - 基本コンポーネントのインストール
   - 影響: アクセシビリティ、開発速度

---

### 📝 P2 - 中（保守性と一貫性）

7. **未使用依存関係の整理**
   - react-query の削除 or 使用開始
   - 影響: バンドルサイズ、技術スタックの明確化

8. **プロジェクト構造の整理**
   - CLAUDE.md との整合性確保
   - ディレクトリ名の統一
   - 影響: 保守性、ドキュメント精度

9. **react-markdown + rehype の導入**
   - シンタックスハイライトの実装
   - 影響: プレビューの視認性

---

### 🔵 P3 - 低（将来的な改善）

10. **独立したプレビューページの実装**
    - `/preview` ページの作成
    - フルスクリーンモード
    - 影響: UXの向上

11. **Zustand への移行検討**
    - Context API の置き換え
    - 影響: パフォーマンス、拡張性

---

## 🎯 次のアクション

### 即座に取り組むべき項目

1. **Step 3-6 の要件定義**
   - 各ステップで何を入力するか明確化
   - WizardFormData 型の拡張

2. **エクスポート機能の最小実装**
   ```typescript
   const downloadAgentsMd = (content: string) => {
     const blob = new Blob([content], { type: 'text/markdown' })
     const url = URL.createObjectURL(blob)
     const a = document.createElement('a')
     a.href = url
     a.download = 'agents.md'
     a.click()
   }
   ```

3. **テンプレート適用機能**
   ```typescript
   // src/app/wizard/page.tsx
   const searchParams = useSearchParams()
   const templateId = searchParams.get('template')

   useEffect(() => {
     if (templateId) {
       applyTemplate(templateId)
     }
   }, [templateId])
   ```

---

## 📌 補足情報

### テストカバレッジ

**現状:** 89/89 テストパス ✅

**テストファイル:**
- `src/__tests__/setup.test.ts` (1 test)
- `src/features/wizard/utils/generate-ai-role-definition.test.ts` (7 tests)
- `src/features/wizard/utils/generate-markdown.test.ts` (9 tests)
- `src/shared/components/code-block.test.tsx` (4 tests)
- `src/features/wizard/hooks/use-wizard-form.test.ts` (8 tests)
- `src/features/wizard/context/wizard-context.test.tsx` (4 tests)
- その他のコンポーネントテスト (56 tests)

**評価:** ✅ 実装済み機能に対するテストカバレッジは良好

---

## 📖 参照ドキュメント

- CLAUDE.md: `/home/user/agents-md-creator/.claude/CLAUDE.md`
- README.md: `/home/user/agents-md-creator/README.md`
- package.json: `/home/user/agents-md-creator/package.json`

---

**作成日:** 2026-01-07
**作成者:** Claude Code
**調査方法:** 静的コード解析、依存関係分析、テスト実行
